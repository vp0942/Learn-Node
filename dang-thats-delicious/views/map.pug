extends layout

block content
  .inner
    h2= title
    .map
      .autocomplete
        input.autocomplate__input(type='text', placeholder='Search for Anything' name='geolocate')
      #map
        p Loading map...